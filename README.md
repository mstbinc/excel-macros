## Excel Macros

This project contains macros that ease the process of developing PFE reports.

All `.vba` files should be defined in the `src/` directory.
They can be automatically compiled into a `PERSONAL.xlsb` file by using the [XLSB compiler](https://github.com/mstbinc/xlsb-compiler).